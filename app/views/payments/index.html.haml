- breadcrumb :payments, student
= link_to t('shared.add_new_item'), new_student_payment_path, class: 'btn btn-default'
%table.table.table-condensed
  %thead
    %tr
      %th= t('student.columns.added')
      %th= t('student.columns.updated')
      %th= t('student.columns.amount')
      %th= t('shared.actions')
  %tbody
    -student.payments.all.order("UPDATED_AT DESC").each do |p|
      = render partial: 'payment', locals: {payment: p}
